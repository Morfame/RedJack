<%= form_with(model: bank) do |form| %>
  <% if bank.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bank.errors.count, "error") %> prohibited this bank from being saved:</h2>

      <ul>
        <% bank.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amt, style: "display: block" %>
    <%= form.number_field :amt %>
  </div>

  <div>
    <%= form.label :play_id, style: "display: block" %>
    <%= form.number_field :play_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
