<%= form_with(model: room) do |form| %>
  <% if room.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
        <% room.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :bank_id, style: "display: block" %>
    <%= form.number_field :bank_id %>
    <%= form.label :play1_id, style: "display: block" %>
    <%= form.number_field :play1_id %>
    <%= form.label :play2_id, style: "display: block" %>
    <%= form.number_field :play2_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
